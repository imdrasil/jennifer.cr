require "../spec_helper"

describe Jennifer::QueryBuilder::Cast do
  described_class = Jennifer::QueryBuilder::Cast

  pending ".new"
  pending "#sql_args"
  pending "#filterable?"
  pending "#set_relation"
  pending "#alias_tables"
  pending "#change_table"
  pending "#as_sql"
end
